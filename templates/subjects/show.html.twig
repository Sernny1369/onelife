{% extends 'base.html.twig' %}

{% block title %}Subjects{% endblock %}

{% block body %}
    <h1>Subjects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subject.id }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ subject.img }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ subject.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_subjects') }}">back to list</a>

    <a href="{{ path('app_subjects_edit', {'id': subject.id}) }}">edit</a>

    {{ include('subjects/_delete_form.html.twig') }}
{% endblock %}
