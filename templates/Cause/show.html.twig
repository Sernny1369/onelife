{% extends "base.html.twig" %}

{% block h1 %}
	{{ cause.title }}
{% endblock %}

{% block body %}

	<a class="btn btn-primary" href="{{ path('app_cause') }}">home</a>

	<table class="table table-hover table-striped table-bordered text-center">
		<tr>
			<th>ID</th>
			<td>{{cause.id}}</td>
		</tr>
		<tr>
			<th>Titre</th>
			<td>{{cause.title}}</td>
		</tr>
        <tr>
            <th>Image</th>
            <td>{{cause.img}}</td>
        </tr>
		<tr>
			<th>Description</th>
			<td>{{cause.description}}</td>
		</tr>

	</table>


{% endblock %}
